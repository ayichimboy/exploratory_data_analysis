[project]
name = "exploratory-data-analysis"
version = "0.1.0"
description = "Data exploration with numpy and pandas for flat files"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=7.2.0",
    "jupyterlab>=4.5.7",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
]
